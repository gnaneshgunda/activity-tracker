# Core numerics
numpy>=2.0
scipy>=1.13

# Data handling
pandas>=2.0,<3.0

# Deep learning — CPU-only build keeps the edge image small.
# For GPU: pip install torch==2.3.0+cu121 --index-url https://download.pytorch.org/whl/cu121
torch>=2.1,<3.0

# llama-cpp-python intentionally excluded — requires C++ compiler (MSVC/MinGW).
# The SLM (Phi-3 Mini) is OPTIONAL. Without it, the router runs in rule-only
# mode and the exemplar narration uses a null fallback. Everything else works.

# Model download from Hugging Face Hub
huggingface-hub>=0.23,<2.0

# Anomaly detection fallback (Isolation Forest)
scikit-learn>=1.3,<2.0

# App / UI
streamlit>=1.35,<2.0

# Testing
pytest>=7.0,<9.0
