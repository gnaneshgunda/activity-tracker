# Core numerics
numpy>=1.24,<2.0
scipy>=1.10,<2.0

# Data handling
pandas>=2.0,<3.0

# Deep learning — CPU-only build keeps the edge image small.
# For GPU: pip install torch==2.3.0+cu121 --index-url https://download.pytorch.org/whl/cu121
torch>=2.1,<3.0

# SLM inference (Phi-3 Mini / Gemma-2 via GGUF)
# CPU-only edge build (recommended):
#   CMAKE_ARGS="-DLLAMA_BLAS=ON -DLLAMA_BLAS_VENDOR=OpenBLAS" pip install llama-cpp-python
# GPU build (if available):
#   CMAKE_ARGS="-DLLAMA_CUBLAS=on" pip install llama-cpp-python
llama-cpp-python>=0.2.57

# Model download from Hugging Face Hub
huggingface-hub>=0.23,<2.0

# Anomaly detection fallback (Isolation Forest)
scikit-learn>=1.3,<2.0

# App / UI
streamlit>=1.35,<2.0

# Testing
pytest>=7.0,<9.0
